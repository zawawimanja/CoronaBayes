<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.awi.coronatracker"
    android:installLocation="auto"
    android:versionCode="02"
    android:versionName="1.0.1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/corona_app_icon"
        android:label="@string/app_name"
        android:logo="@mipmap/corona_app_icon"
        android:roundIcon="@mipmap/corona_app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/MyToolbarTheme">
        <activity android:name=".notes.NotesActivity"></activity>
        <activity android:name=".test.Main2Activity" />
        <activity android:name=".auth.SignInActivity" />
        <activity android:name=".intro.WelcomeActivity" />
        <activity android:name=".intro.IntroductionMemory" />
        <activity android:name=".intro.SplashActivity"></activity>
        <activity android:name=".QR.QrCreateActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>